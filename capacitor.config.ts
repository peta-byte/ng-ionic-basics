import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ng-ionic-basics',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
